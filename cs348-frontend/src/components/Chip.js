import React from 'react';

const Chip = (props) => {
    return (
        <div class="fm-chip">
            <span class="fm-chip-label">{props.label}</span>
        </div>
    );
}

export default Chip;
